package com.majid2851.charts.domain.model

/**
 * Common enums shared across different chart types
 */

enum class ChartType {
    LINE,
    BAR,
    PIE,
    AREA,
    SCATTER,
    RADAR,
    COMPOSED,
    RADIAL_BAR
}

enum class PointShape {
    CIRCLE,
    SQUARE,
    TRIANGLE,
    DIAMOND,
    CROSS,
    STAR
}

enum class ChartOrientation {
    HORIZONTAL,
    VERTICAL
}

enum class LabelPosition {
    INSIDE_TOP,
    INSIDE_BOTTOM,
    INSIDE_LEFT,
    INSIDE_RIGHT,
    OUTSIDE_TOP,
    OUTSIDE_BOTTOM,
    OUTSIDE_LEFT,
    OUTSIDE_RIGHT,
    OUTSIDE,
    INSIDE_BOTTOM_RIGHT,
    CENTER
}











